package ch.onepass.onepass.model.event

import org.junit.Assert.assertNotNull
import org.junit.Test

class EventRepositoryFirebaseTest {
  // THIS IS A VIRTUAL TEST FOR AN EMPTY CLASS. REMOVE DURING DEVELOPMENT.
  @Test
  fun instantiateEventRepositoryFirebase() {
    val repository = EventRepositoryFirebase()
    assertNotNull(repository)
  }
}
