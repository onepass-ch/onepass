package ch.onepass.onepass.model.auth

import org.junit.Assert.assertNotNull
import org.junit.Test

class AuthRepositoryFirebaseTest {
  // THIS IS A VIRTUAL TEST FOR AN EMPTY CLASS. REMOVE DURING DEVELOPMENT.
  @Test
  fun instantiateAuthRepositoryFirebase() {
    val repo = AuthRepositoryFirebase()
    assertNotNull(repo)
  }
}
