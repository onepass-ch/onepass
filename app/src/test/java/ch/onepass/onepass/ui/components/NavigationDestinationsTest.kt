package ch.onepass.onepass.ui.components

import org.junit.Assert.assertNotNull
import org.junit.Test

class NavigationDestinationsTest {
  // THIS IS A VIRTUAL TEST FOR AN EMPTY CLASS. REMOVE DURING DEVELOPMENT.
  @Test
  fun referenceNavigationDestinations() {
    // Ensure object is touched for coverage
    val ref = NavigationDestinations
    assertNotNull(ref)
  }
}
