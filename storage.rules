rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    // Testing mode: Allow all read and write operations for authenticated users
    match /{allPaths=**} {
      allow read, write: if request.auth != null;
    }
  }
}
